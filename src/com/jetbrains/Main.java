package com.jetbrains;

public class Main {

    public static void main(String[] args) {
        Deck deck = new Deck();
            deck.shuffle(deck.deck);
        deck.head(deck.deck);
            deck.pick(deck.deck);
            deck.hand(deck.deck);

        String a = "";
    }
}
